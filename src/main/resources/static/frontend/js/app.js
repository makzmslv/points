'use strict';
// Declare app level module which depends on filters, and services
var points = angular.module('points', [ 'ui.bootstrap', 'ngRoute', 'ngCookies', 'restServices', 'homeController']);

points.config([ '$routeProvider', '$locationProvider', function($routeProvider, $locationProvider) {


} ]);
